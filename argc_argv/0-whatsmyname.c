#include <stdio.h>

/**
 * main - prints its name
 *
 * @argc: no used
 * @argv: string contain
 *
 * Return: always 0.
 */

int main(__attribute__((unused))int argc, char **argv)
{
	printf("%s\n", argv[0]);

	return (0);
}

