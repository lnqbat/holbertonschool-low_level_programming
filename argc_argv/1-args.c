#include <stdio.h>

/**
 * main - rints the number of arguments passed into it
 *
 * @argc: count number of arguments .
 * @argv: not used
 *
 * Return: Always 0.
 */

int main(int argc, __attribute__((unused))char **argv)
{
	printf("%d\n", argc);
	return (0);
}
